# -----------------------------------------------------------------------------
# Cache — BuildCacheFromEnv (requires jardisadapter/cache)
# -----------------------------------------------------------------------------
# Delivered inactive: everything commented, the kernel carries null for the
# cache. Uncomment to activate. Layers are tried in order, first hit wins;
# the "redis" layer needs .env.redis and the cache compose profile.
# -----------------------------------------------------------------------------

#CACHE_LAYERS=memory,redis
#CACHE_NAMESPACE=app
# only read for the "db" layer, defaults to "cache" if unset
#CACHE_DB_TABLE=cache
